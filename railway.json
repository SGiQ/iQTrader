{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @sgiq/shared build && pnpm --filter @sgiq/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @sgiq/api run db:migrate:prod && pnpm --filter @sgiq/api run start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
